# TeamControl

Instruções de Instalação
1. Execute: composer install / php composer.phar install
2. Execute no seu banco de dados postgres o migration.sql, ou crie o database 'teamcontrol' e use o migration.sqlbk para
restaurar o backup
